# Kopia jako .env.production.test (plik jest w .gitignore — nie commituj haseł).
# Dane logowania admina: patrz DANE.md w katalogu głównym repozytorium.

PLAYWRIGHT_BASE_URL=https://magia-pod.vercel.app

TEST_USER_EMAIL=admin@magia.pl
TEST_USER_PASSWORD=

# Opcjonalnie: konkretny slug (domyślnie — pierwsza wycieczka z wolnymi miejscami z API)
# PRODUCTION_TRIP_SLUG=test-maroko-split

# Pełny test B1.2 z Paynow sandbox (tworzy rezerwację + płatność)
# PRODUCTION_E2E_PAYMENT=1
